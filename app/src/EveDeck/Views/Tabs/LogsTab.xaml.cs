using System.Windows.Controls;
using UserControl = System.Windows.Controls.UserControl;

namespace EveDeck.Views.Tabs;

// Content of the Logs TabItem, split out of MainWindow.xaml. No code-behind handlers.
public partial class LogsTab : UserControl
{
    public LogsTab()
    {
        InitializeComponent();
    }
}
